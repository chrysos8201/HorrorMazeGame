#include "pch.h"
#include "MyError.h"
#include <stdio.h>
#include <errno.h>
void PrintError(const char* msg)
{
	////fprintf(stderr, "Error by : \"%s\", errno:%d\n", msg, errno);
	////perror("open");
	//fputs(msg, stderr);
	//exit(1);
}
